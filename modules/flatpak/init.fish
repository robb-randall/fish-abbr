abbr -ag fpi		flatpak install --user
abbr -ag fpl		flatpak list --columns=$FLATPAK_COLUMNS
abbr -ag fpr		flatpak run
abbr -ag fps		flatpak search --user --columns=$FLATPAK_COLUMNS
abbr -ag fpu		flatpak update
